{
  id: @child.id,
  name: @child.name,
  birthdate: @child.birthdate,
  children_milestones: @child.children_milestones.map do |milestone|
    {
      date_accomplished: milestone.date_accomplished,
      
    }
  end,
  milestones: @child.milestones.map do |milestone|
    {
      milestone_category: milestone.milestone_category.category,
      description: milestone.description,
    }
  end
}